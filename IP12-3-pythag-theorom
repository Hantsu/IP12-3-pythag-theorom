/******************************************************************************
 Filename:      IP12 - Functions Programming Sprint
 Author:        Max Taylor
 Navigators:    Robert C.
 Date:          2/14/19
 
 Goal:          Write a program that accepts the lengths of three sides of a triangle as inputs. The program
                output should indicate whether or not the triangle is a right triangle. Recall from the
                Pythagorean theorem that in a right triangle, the square of one side (the long side) equals the
                sum of the squares of the other two sides. ( c2 = a2 + b2)
*******************************************************************************/
#include <iostream>
#include <cmath>
using namespace std;

int main(){
    
    int vara;       //variable for a, Given by user input
    int varb;       //variable for b, Given by user input
    int varc;       //variable for c, Given by user input
    int ans;

    cout << "Please enter a variable for a, b and c.\n";
    cout << "A: ";
    cin >> vara;
    cout << endl;
    
    cout << "B: ";
    cin >> varb;
    cout << endl;
    
    cout << "C: ";
    cin >> varc;
    cout << endl;
    
    varc = pow(varc, 2);
    
    ans = pow(vara, 2) + pow(varb, 2);      // c^2 value
    
    
    if(varc == ans)
    {
        cout << "Your triangle is a right triangle!";
    }
    else
    {
        cout << "Your triangle is not a right triangle. Sorry.";
    }
    
return 0;
        
}
